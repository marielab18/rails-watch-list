<%= simple_form_for([@list, @bookmark]) do |f| %>
  <%= f.input :comment %>
  <%= f.association :movie, collection: Movie.all.order(:title) %>
  <%= f.button :submit, class: "btn btn-flat" %>
<% end %>

<%= link_to "See #{@list.name}", list_path(@list), class: "btn btn-ghost" %>
